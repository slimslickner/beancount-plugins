metadata:
  transaction:
    source_payee:
      label: "Original payee name from import"
      type: string
      required: false
    source_account:
      label: "Source account for transfer transactions"
      type: string
      required: false
    import_date:
      label: "Date the transaction was imported"
      type: date
      required: false

  posting:
    tag:
      label: "Posting-level categorization"
      type: string
      allowed_values: [personal, business, reimbursable]
      required: false
    receipt_id:
      label: "Reference to receipt or supporting document"
      type: string
      required: false

  plugin_exceptions:
    - allowed_prefix: "_"  # Beancount internal keys
    - allowed_keys: [predicted_payee, matched_transfer_account, matched_transfer_date]  # Smart importer and plugin keys
